# The deployment configuration is compiled straight into the harness, along with the raw
# file class and the INI reader it stands on. It lives outside code/ so that the recursive
# glob building OpenTS cannot pick this target's entry point up.
add_executable(DeploymentConfig
    "${CMAKE_CURRENT_SOURCE_DIR}/deploymentconfigcontract.cpp"
    "${CMAKE_SOURCE_DIR}/code/deploymentconfig.cpp"
    "${CMAKE_SOURCE_DIR}/code/_deploymentconfig.cpp"
    "${CMAKE_SOURCE_DIR}/code/bfiofile.cpp"
    "${CMAKE_SOURCE_DIR}/code/rawfile.cpp"
    "${CMAKE_SOURCE_DIR}/code/file.cpp"
    "${CMAKE_SOURCE_DIR}/code/file_posix.cpp"
    "${CMAKE_SOURCE_DIR}/code/file_win.cpp"
    "${CMAKE_SOURCE_DIR}/code/dbgprint.cpp"
    "${CMAKE_SOURCE_DIR}/code/ini.cpp"
    "${CMAKE_SOURCE_DIR}/code/utf8.cpp"
    "${CMAKE_SOURCE_DIR}/code/b64pipe.cpp"
    "${CMAKE_SOURCE_DIR}/code/b64straw.cpp"
    "${CMAKE_SOURCE_DIR}/code/base64.cpp"
    "${CMAKE_SOURCE_DIR}/code/buff.cpp"
    "${CMAKE_SOURCE_DIR}/code/crc.cpp"
    "${CMAKE_SOURCE_DIR}/code/cstraw.cpp"
    "${CMAKE_SOURCE_DIR}/code/int.cpp"
    "${CMAKE_SOURCE_DIR}/code/mpmath.cpp"
    "${CMAKE_SOURCE_DIR}/code/pipe.cpp"
    "${CMAKE_SOURCE_DIR}/code/pk.cpp"
    "${CMAKE_SOURCE_DIR}/code/straw.cpp"
    "${CMAKE_SOURCE_DIR}/code/trim.cpp"
    "${CMAKE_SOURCE_DIR}/code/xpipe.cpp"
    "${CMAKE_SOURCE_DIR}/code/xstraw.cpp"
)

target_compile_features(DeploymentConfig PRIVATE cxx_std_20)

# The generated build stamp comes with dbgprint.cpp, so this target needs it too.
target_include_directories(DeploymentConfig PRIVATE
    "${CMAKE_SOURCE_DIR}/code"
    "${OPENTS_GENERATED_DIR}"
)

add_dependencies(DeploymentConfig OpenTSBuildStamp)

target_compile_definitions(DeploymentConfig PRIVATE WIN32 _WINDOWS _MBCS NOMINMAX)

target_compile_options(DeploymentConfig PRIVATE
    $<$<CONFIG:Debug>:/MTd /EHsc /Zc:__cplusplus /utf-8>
    $<$<CONFIG:Release>:/MT /EHsc /Zc:__cplusplus /utf-8>
)

target_link_libraries(DeploymentConfig PRIVATE kernel32 user32 shell32)

set_target_properties(DeploymentConfig PROPERTIES
    RUNTIME_OUTPUT_DIRECTORY "${CMAKE_BINARY_DIR}/bin"
)

add_test(NAME deploymentconfig COMMAND DeploymentConfig)
